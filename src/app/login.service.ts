import { HttpClient } from '@angular/common/http';
// import { loginData } from './app.component'

export class LoginService {
    createUser( httpClient:HttpClient){
        // return this.client.post(`${DATA_ACCESS_PREFIX}`, traveller);
      }
}